{
  "name": "React-Basic-Setup",
  "version": "5.0.0",
  "description": "Simplest package.json setup for React projects",
  "author": "Rory Ashford <rory@roikles.com>",
  "config": {},
  "scripts": {
    "reactify": "cross-env NODE_ENV=production && browserify -t [ babelify ] main.js -o index.js && uglifyjs index.js -o index.js"
  },
  "dependencies": {
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "cross-env": "^1.0.8",
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  }
}
